services:
  mansio:
    build: .
    ports:
      - "127.0.0.1:${MANSIO_PORT:-8080}:8080"
    volumes:
      - mansio-data:/data
      - mansio-home:/home/mansio
    restart: unless-stopped

volumes:
  mansio-data:
  mansio-home:
